/* package facilityUseTest;

import database.DBConnector;
import facilityBase.Facility;
import java.util.*;
import java.sql.*;
import java.time.*;

public class facilityUseTest;

@Test1{
        {
public void setPurposeOfUse;
        SetPurposeOfUse setPurposeOfuse = new setPurposeOfuse;
        assert.AssertEquals(purposeOfUse,Facility.purposeOfUse());

public void setStartDate(lLocalDate startDate);
        SetStartDate( LocalDate startDate) setStartDate( LocalDate startDate) = new setStartDate( LocalDate startDate);
        assert.AssertEquals(start,Facility.startDateTime());

public void setEndDate(LocalDate endDate);
        SetEndDate(LocalDate endDate) setEndDate(LocalDate endDate) = new setEndDate(LocalDate endDate);
        assert.AssertEquals(end,Facility.endDateTime());
        }
        {
public void setInspectionDate(Local inspectionDate);
        SetInspectionDate(Local inspection Date)  setInspectionDate(Local inspection Date) = new setInspectionDate(Local inspection Date);
        assert.AssertEquals(inspectionDate, Facility.inspectionDate());

public void setInspectionDetails(String inspectionDetails)
        SetInspectionDetails(String inspectionDetails) setInspectionDetails(String inspectionDetails) = new setInspectionDetails(String inspectionDetails)
        assert.AssertEquals(inspectionDetails, Facility.inspectionDetails());

        }




        } */